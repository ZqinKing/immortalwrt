# With firmware RPS and the Linux RPS fan-out, bursts to host-terminated
# flows land on the per-CPU receive backlogs; the 1000-packet default
# overflows at line-rate bursts (softnet_stat column-2 drops). 4096 absorbs
# them with no measured drops at ~1.5 Gbit/s offered.
net.core.netdev_max_backlog=4096

# Default RPS mask for late-created delivery netdevs, especially Wi-Fi VAPs
# recreated after DFS/CAC. Supported IPQ807x/IPQ60xx boards are A53 quads, so
# this preserves the original cores 1-3 fan-out. nss-up and the hotplug helper
# still recompute the runtime mask from online non-boot CPUs when they run.
net.core.rps_default_mask=e
